import styled from "styled-components";
import { colors } from "../../../styleguide/utilities";

const StyledButton = styled.button`
  background: ${colors.white};
  outline: none;
  border: none;
  width: 100%;
  padding: 0;
  cursor: pointer;
`;

export default StyledButton;
