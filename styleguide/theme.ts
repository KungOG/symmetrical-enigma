import { colors } from './utilities';

export const lightTheme = {
  body: `${colors.white}`,
  text: `${colors.black}`,
};

export const darkTheme = {
  body: `${colors.black}`,
  text: `${colors.white}`,
};